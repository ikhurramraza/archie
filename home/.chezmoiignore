# Source-only payloads consumed by .chezmoiscripts — never deployed into $HOME.
mpvnet
mpvnet/**
windows-terminal
windows-terminal/**

{{ if not (has .profile (list "cov" "dort" "personal")) -}}
.ssh/box
.ssh/box.pub
{{ end -}}

{{ if ne .profile "personal" -}}
.config/mpv
.config/mpv/**
.config/yt-dlp
.config/yt-dlp/**
{{ end -}}
