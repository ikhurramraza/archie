# Source-only payloads consumed by .chezmoiscripts — never deployed into $HOME.
mpvnet
mpvnet/**
windows-terminal
windows-terminal/**

{{ if not (has .profile (list "personal" "cov" "dort")) -}}
.ssh/box
.ssh/box.pub
{{ end -}}
