skip_output:
  - meta
  - summary
  - success
  - execution

pre-commit:
  parallel: true
  commands:
    shfmt:
      files: git diff --name-only scripts/ config/zsh/ config/i3/scripts/
      exclude: ".p10k.zsh|.zshrc"
      run: shfmt -i=2 -d {files}

    stylua:
      files: git diff --name-only **/*.lua
      run: stylua --check {files}

    snapshot-neovim:
      files: git diff --name-only config/nvim/lua/plugins.lua
      run: bin/snapshot neovim
